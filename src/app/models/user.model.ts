export type loginUser = {
    email: string;
    password: string;
}

export type registerUser = {
    email: string;
    password: string;
    username: string;
    firstName: string;
    lastName: string;
}